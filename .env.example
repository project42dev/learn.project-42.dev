# Public API origin only. OIDC endpoints, client credentials, and session keys
# belong to the account API and must never be compiled into this browser bundle.
NEXT_PUBLIC_PROJECT42_API_ORIGIN=https://api.example.invalid
